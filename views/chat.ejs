<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="../public/style.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <title>/chat --- chat.ejs</title>
</head>
<body>

<%- include('nav.html')%>

<div class="container p-4 detail">

    <div class="row">

      <div class="col-3">
        <h4>내가 포함된 chatroom컬렉션</h6>
        <p>member에 나(요청.user._id)가 있는 chatroom</p>
        <ul class="list-group chat-list">
          <% for (let i=0; i < data.length ; i++) { %>
          <li class="list-group-item" data-id="<%data[i]._id%>">
            <h6><%=i+1 %>. <%= data[i].title %></h6>
            <h6 class="text-small">_id(parent): <%= data[i]._id %></h6>
          </li>
          <% } %>
         </ul>
       </div>

       <div class="col-9 p-0">
         <div class="chat-room">
          <h1>/chat.ejs</h1>
          <h2><%=user.id%></h2>
          <h4>현재 접속한 나는 누구? <%= data[0].member[1] %></h4>
          <ul class="list-group chat-list">

            <% for (let i=0; i < data.length ; i++){ %>
              <!-- // TODO: data-id => data-set -->
              <li class="list-group-item" data-id="<%= data[i]._id %>">
                <h6>(parent):chatroom 컬렉션에서 member=요청.user_id</h6>
                <h6>member: <%= data[i].member %></h6>
                <h6><%=i%>  <%= data[i].title %> </h6>     
                <h6 class="text-small"> <%= data[i].member[0] %> </h6>
              </li>
            <% } %>
          </ul> 
          <!-- // FIXME: 메세지 전송 -->
          <div class="input-group">
            <!-- // TODO: #chat-input.val() -->
            <input class="form-control" id="chat-input"> 
            <button class="btn btn-secondary" id="send">전송(id=chat-input)</button>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.js"></script>

<script>
  // TODO: 전송 클릭시 동작.1) 서버로 게시물 전송
  // - 부모 게시글이 먼가? 왼쪽 채팅방이 부모.(클릭하면 전달)
  //1
  var 지금누른채팅방id;
  $('.list-group-item').click(function(){
    지금누른채팅방id = this.dataset.id;
  })
  //2
  $('#send').click(function(){
    var 채팅내용 = $('#chat-input').val();
    var 보낼거 = {
      parent : 지금누른채팅방id,
      content : 채팅내용,
    };

    //3 message 에 저장하라고, 서버에 post요청. $.post
    $.post('/message', 보낼거).then((a)=>{
      console.log(a)
    });
  });
//4
var 지금누른채팅방id;

$('.list-group-item').click(function (){
   $(this).css('background-color', '#eee');
   지금누른채팅방id = $(this).attr('data-id');
   console.log(지금누른채팅방id);
});
  
</script>

  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
</body>
</html>
